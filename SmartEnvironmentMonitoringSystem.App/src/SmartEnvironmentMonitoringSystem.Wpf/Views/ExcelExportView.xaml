<UserControl x:Class="SmartEnvironmentMonitoringSystem.Wpf.Views.ExcelExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Excel 导出" Style="{StaticResource PageTitleText}" />
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource MutedText}" />
        </StackPanel>

        <Border Grid.Row="1" Margin="0,18,0,18" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="开始" VerticalAlignment="Center" Style="{StaticResource FieldLabelText}" />
                <DatePicker Grid.Column="1" Margin="8,0,18,0" SelectedDate="{Binding StartDate}" />
                <TextBlock Grid.Column="2" Text="结束" VerticalAlignment="Center" Style="{StaticResource FieldLabelText}" />
                <DatePicker Grid.Column="3" Margin="8,0,18,0" SelectedDate="{Binding EndDate}" />
                <Button Grid.Column="5" Content="导出" Style="{StaticResource PrimaryButtonStyle}" Width="100" Command="{Binding ExportCommand}" />

                <TextBlock Grid.Row="1" Text="文件" Margin="0,16,0,0" VerticalAlignment="Center" Style="{StaticResource FieldLabelText}" />
                <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4" Margin="8,16,12,0" Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Row="1" Grid.Column="5" Margin="0,16,0,0" Content="选择" Style="{StaticResource SecondaryButtonStyle}" Width="100" Command="{Binding BrowseCommand}" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource PanelBorderStyle}" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="工作表" Style="{StaticResource SectionTitleText}" />
                <UniformGrid Margin="0,16,0,0" Columns="4">
                    <Border Margin="0,0,12,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="14">
                        <TextBlock Text="温湿度数据" Style="{StaticResource StatusValueText}" />
                    </Border>
                    <Border Margin="0,0,12,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="14">
                        <TextBlock Text="空气质量数据" Style="{StaticResource StatusValueText}" />
                    </Border>
                    <Border Margin="0,0,12,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="14">
                        <TextBlock Text="告警记录" Style="{StaticResource StatusValueText}" />
                    </Border>
                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="14">
                        <TextBlock Text="监测报告" Style="{StaticResource StatusValueText}" />
                    </Border>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
